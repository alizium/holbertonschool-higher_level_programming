#!/usr/bin/node

/**
 * const = variable permanent
 * myVar = nom de la variable
 * "JavaScript is amazing" = texte contenu dans la variable
 */
const myVar = "JavaScript is amazing"

/*console.log = afficher le texte de la variable myVar*/
console.log(myVar);