#!/usr/bin/node

// on crée un tableau avec les 3 phrases
const lines = ["C is fun", "Python is cool", "JavaScript is amazing"];

// on fait une boucle pour les afficher une par une
for (const line of lines) {
  console.log(line); // Une seule console.log dans la boucle
}
